---
alwaysApply: true
---
# Dashboard rules — Portal 2026

## Auto-import e aliases (regra dura)

Nuxt 4 auto-importa por convencao. Nao usar `import` manual quando o auto-import existe.

### Vue (auto)
- `ref`, `reactive`, `computed`, `watch`, `watchEffect`, `nextTick`, `onMounted`, `onBeforeUnmount`, `onUnmounted`, `defineComponent`, etc. **NUNCA** importar de `'vue'`. Apenas `defineProps`/`defineEmits`/`defineModel`/`defineSlots` sao macros (sem import).

### Componentes (auto)
- Tudo em `app/components/**` vira tag por path + filename, com **deduplicacao de segmentos iguais**:
  - `app/components/dashboard/SidebarNav.vue` -> `<DashboardSidebarNav />`
  - `app/components/dashboard/leads/LeadsTable.vue` -> `<DashboardLeadsTable />` (Leads dedupada)
  - `app/components/dashboard/charts/SourceDonut.vue` -> `<DashboardChartsSourceDonut />`
- **NUNCA** `import X from '~/components/...'`. Use a tag.
- Renomeie o ficheiro para casar com o nome do diretorio quando quiser nome curto (ex.: `LeadsDrawer.vue` em `leads/` para `<DashboardLeadsDrawer />` em vez de `<DashboardLeadsLeadDrawer />`).
- Para dynamic component, use `import { Foo } from '#components'`.

### Composables / utils (auto)
- Qualquer `useXxx` em `app/composables/**` e funcao em `app/utils/**` ja e auto-imported em qualquer ficheiro `app/`.
- **NUNCA** `import { useRole } from '~/composables/useRole'`.

### Helpers Nuxt (auto)
- `useFetch`, `useAsyncData`, `useRoute`, `useRouter`, `useState`, `useCookie`, `useRuntimeConfig`, `definePageMeta`, `defineNuxtPlugin`, `defineNuxtRouteMiddleware`, `navigateTo`, `createError`, `useSupabaseUser`, `useSupabaseClient` (do modulo `@nuxtjs/supabase`).

### Server (Nitro auto)
- `defineEventHandler`, `getQuery`, `getRouterParam`, `readBody`, `createError`, `setResponseStatus`, `getRequestHeader`, `useRuntimeConfig` -- nunca importar.
- Tudo em `server/utils/**/*.ts` e auto-imported em qualquer `server/api/**/*.ts`. Ex.: `serverSupabaseServiceRole(event)` e `requireAdmin(event)` sem import.
- `serverSupabaseClient` vem de `'#supabase/server'` (alias do modulo). Esse e o **unico** import necessario nos handlers Supabase.

### Path aliases (Nuxt 4)
- `~/` ou `@/` -> `app/` (srcDir).
- `~~/` ou `@@/` -> raiz do projeto (rootDir).
- **PROIBIDO** usar `..` para sair do diretorio. Sempre usar alias.
- Para tipos de endpoints Nitro em SFCs/composables: `import type { Foo } from '~~/server/api/dashboard/.../foo.get'`.

### Quando importar manualmente (excecoes)
- Bibliotecas npm: `import { Icon } from '@iconify/vue'`, `import gsap from 'gsap'`, `import Sortable from 'sortablejs'`.
- Componentes do Unovis: `import { VisXYContainer, VisArea } from '@unovis/vue'` (nao auto).
- Tipos do TypeScript: `import type { Database } from '~/types/database.types'`.

## Identidade visual (obrigatoria)
- Tokens HSL via Tailwind (`bg-bg`, `bg-surface`, `text-text-primary`, `text-muted`, `border-stroke`, `text-accent`). **Proibido hex inline** em componentes; adiciona token novo em `app/assets/css/main.css` antes.
- Titulos de seccao: `font-display italic` (Instrument Serif) com `text-text-primary` ou `/88`.
- Corpo: `font-sans` (Inter).
- Superficies elevadas: `.glass-surface`.
- Destaques: `.accent-gradient` com `bg-clip-text text-transparent`.
- Dark mode: `<html data-theme="dark">`. Nao mexer em `:root` -- estender `[data-theme='dark']`.
- Sem video/autoplay na dashboard.

## Arquitectura
- Paginas em `app/pages/dashboard/**` declaram `definePageMeta({ layout: 'dashboard' })` + `middleware: ['role']` + `role: 'admin' | UserRole[]`.
- Auth global em `app/middleware/auth.global.ts` so intercepta `/dashboard/**`.
- Authorization em `useRole().can(action)` e `isAdmin/isEditor`.
- **Middlewares usam `useRole().ensureProfile()`** (nao `load(true)` direto) para sobreviver ao race do session restore no primeiro login pos-OTP. Se `profile` nao chega apos timeout, redirecionar para `/confirm` ou `/login` -- **nunca 403 por race**.
- `/confirm` aguarda `ensureProfile()` antes de `navigateTo`, e respeita `?redirect=`.
- Pagina de erro global e `app/error.vue` com identidade da dashboard (glass + serif).
- Tipos em `app/types/auth.ts` e `app/types/database.types.ts` (regen via Supabase MCP).

### DDD + Layers (AI-first)
- **Server DDD por contexto**: `server/domain/dashboard/{projects,sprints,tasks}.service.ts`.
- Nao usar bridge `server/utils/dashboardProjectsService.ts` (removida). Comandos da app-layer (`server/application/**`) chamam ports/repositorios.
- Handlers `server/api/dashboard/**` devem ser finos: parse/guard/chamada de **application commands** (`server/application/**`), sem acoplamento direto a dominio/repositorio.
- **Nuxt Layers**: preparar em `layers/1-base` (fundacoes) e `layers/2-dashboard` (dominio dashboard) com migracao incremental; nao mover tudo de uma vez.
- Regra de precedencia de Layers:
  1. `app/**` (override final sobre todos os layers),
  2. ordem em `extends` no `nuxt.config` — entradas **mais à direita** prevalecem entre layers (ex.: `./layers/2-dashboard` sobrepõe `./layers/1-base`),
  3. config/projeto base.

### Contratos obrigatorios por camada
- `server/domain/**`: invariantes, regras de negocio, sem dependencia de HTTP/Nitro.
- `server/application/**`: orchestracao de use-cases, authz/audit cross-cutting, sem UI concerns.
- `server/infrastructure/**`: adapters Supabase/Drizzle/repositories, sem regra de negocio de dominio.
- `server/api/**`: somente adaptacao HTTP (params/body/status), sem regra rica.
- `layers/2-dashboard/**` e `app/pages/dashboard/**`: composicao e apresentacao; persistencia apenas via store/API.

### Regras de migração do refactor geral
- Migrar por bounded context (access, leads, projects, tracking, portfolio), nunca por "tipo de arquivo" global.
- Toda etapa deve manter bridge de compatibilidade (facades/re-exports) ate concluir migracao do contexto.
- Nao aceitar PR de refactor sem:
  - update em `.planning/codebase/DDD-LAYERS-BLUEPRINT.md`,
  - update em `.planning/STATE.md`,
  - teste cobrindo o boundary alterado.

## Regra de ouro -- service role
- `NUXT_SUPABASE_SECRET_KEY` (legacy `SUPABASE_SERVICE_KEY`) so existe no server.
- INSERT/UPDATE/DELETE de risco sempre via handler Nitro com `serverSupabaseServiceRole(event)`.
- Clients usam anon key + dependem de RLS.
- Endpoints admin usam `requireAdmin(event)` no inicio para devolver 401/403 antes da logica.

## Rate-limit de email (Supabase)
- SMTP default do Supabase libera ~3-4 magic links/hora por projeto -- testes repetidos disparam `email rate limit exceeded`.
- Em dev, preferir login por **email + senha** (configurar `encrypted_password` via MCP com `crypt(..., gen_salt('bf'))` para usuarios de teste).
- Em producao, configurar SMTP proprio (Resend/SES/Mailtrap) em Supabase Dashboard -> Auth -> SMTP.
- **Nunca commitar senhas de dev em codigo versionado** -- apenas em `.planning/STATE.md` (local, git-ignored quando aplicavel).

## Supabase / RLS
- Migrations numeradas (`0001_auth.sql`, `0002_tracking.sql`, ...).
- Sempre `ENABLE ROW LEVEL SECURITY` + policies explicitas por role.
- Verifica policies para `{admin, editor, viewer}` antes de expor endpoint.
- Helpers ja existentes (reusa!): `public.is_admin()`, `public.is_editor_or_admin()`, `public.can_access_project(uuid)`. Todos `SECURITY DEFINER` com `search_path = public`.
- **Nunca** escrever `exists (select 1 from public.profiles where role = 'admin')` diretamente em policies -- dispara recursao RLS (erro 500 "infinite recursion detected in policy for relation 'profiles'"). Usa `public.is_admin()`.
- Toda nova policy que precise distinguir role **chama a helper apropriada**, nao faz subquery inline em `profiles`.

### Supabase + Drizzle (hibrido)
- Estrategia oficial atual: `Drizzle -> fallback Supabase service-role` nos services de dashboard.
- Se `DATABASE_URL` nao existe, o fluxo deve funcionar integralmente por Supabase.
- Auditoria de mutacoes criticas sempre via `public.log_audit()` (`server/utils/audit.ts`).
- Nenhum write sensivel direto no cliente; cliente conversa apenas com Nitro.

### Optimistic load/update (Pinia)
- Estados de dashboard devem privilegiar UX responsiva com:
  - update otimista local,
  - rollback em erro,
  - reconciliacao por refetch seletivo.
- Se a mutacao falhar, restaurar snapshot local antes de propagar erro.

## Layout
- Grid principal `grid-cols-12 gap-4`. Mobile stack.
- Sidebar fixa 240px em md+.
- TopBar com toggle de tema e pill de user.

## Bibliotecas permitidas
- Charts: `@unovis/vue` (leve, SSR-safe). **Nao introduzir** chart.js/echarts sem plano.
- Drag-and-drop: `sortablejs`.
- Icons: `@iconify/vue` com conjunto `lucide:*`.

## Testes
- Cada composable novo -> Vitest unit.
- Cada pagina critica -> 1 E2E minimo.
- Mock `**/api/dashboard/**` em E2E para nao depender de Supabase real.
- Atualizar `.planning/codebase/TESTING.md` quando adicionar suite.

## Checklist antes de commitar
- [ ] `npm run typecheck` verde (nuxt typecheck local, sem npx).
- [ ] `npm run test` verde.
- [ ] Alterações em copy/média/landing relevantes: considerar **Human UAT (TEST-06)** em `.planning/codebase/TESTING.md`.
- [ ] Sem `import` redundante (vue, componentes locais, server utils).
- [ ] Sem path com `..` -- so aliases `~/` ou `~~/`.
- [ ] Sem hex inline ou cor fora dos tokens.
- [ ] Sem service key no bundle cliente.
- [ ] RLS testada para os tres roles.
- [ ] `.planning/STATE.md` atualizado se fechaste fase.

## Checklist arquitetural de PR (obrigatorio)
- [ ] `server/api/**` continua fino (parse/guard/dispatch), sem regra rica.
- [ ] `server/application/**` orquestra use-cases; sem detalhes de driver/persistencia.
- [ ] `server/domain/**` preserva invariantes; sem dependência HTTP/Nitro.
- [ ] `server/infrastructure/**` contem adapters/repositories; sem regra de negocio.
- [ ] Mudancas de boundary atualizam `.planning/codebase/CONVENTIONS.md` e `.planning/codebase/TESTING.md`.
- [ ] PR com refactor DDD inclui testes do boundary alterado (contract/unit/e2e conforme escopo).
