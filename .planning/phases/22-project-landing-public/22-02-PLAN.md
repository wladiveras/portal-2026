# 22-02 — Rotas públicas + commands publish/draft

## Goal

PC-05 + PC-06: handlers finos; página pública `/p/[slug]` (ou prefixo acordado).

## Tasks

1. Application commands `saveDraft`, `publishLanding`, query pública por slug.
2. Nitro routes + testes smoke handler público (published only).
3. Integração captura lead com `project_id`.

## Verification

- E2E ou contrato handler público sem auth.
- Sem service key no bundle cliente.
