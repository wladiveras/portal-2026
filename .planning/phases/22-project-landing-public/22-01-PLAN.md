# 22-01 — Schema project_landing + migração

## Goal

PC-04: tabela `project_landing` 1:1 com projeto, slug único, JSON blocos.

## Tasks

1. DDL + índices (`slug` unique partial por scope definido no REQ).
2. Tipos TS + Drizzle mirror opcional.

## Verification

- Migração aplicável; contrato documentado em REQ §5.
