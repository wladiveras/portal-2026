# 25-01 — Drizzle espelho + gates qualidade

## Goal

PC-11 + PC-12: alinhar `server/db/schema.ts` às novas tabelas aplicáveis; testes contrato.

## Tasks

1. Atualizar schema Drizzle para org/landing (onde uso Drizzle já existir).
2. Manter leads/access **Supabase-first** até paridade total (INFRA).
3. Documentar em STATE/boundary tests.

## Verification

- `npm run test` + `npm run typecheck` verdes.
