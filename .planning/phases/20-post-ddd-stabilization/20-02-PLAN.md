# 20-02 — Simetria portfolio + superfície pública de tracking

## Objetivo

Alinhar os últimos handlers “estáticos” ao padrão **transport → application → (domínio|infra)** para narrativa única em PRs e auditorias.

## Escopo

- `GET /api/portfolio`: extrair payload para `server/application/dashboard/portfolio/queries.ts` (ex.: `qryPortfolioPayload()` retornando o objeto atual ou fonte futura CMS/DB).
- Handler `server/api/portfolio.get.ts` permanece fino (uma chamada à query).
- Revalidar `tests/unit/server/portfolio.get.spec.ts` contra a nova superfície (mesmo shape HTTP).

## Fora de escopo

- Persistência real do portfolio (continua estático até decisão de produto).

## Critérios de aceite

- [x] `npm run typecheck` e `npm run test` verdes.
- [x] Nenhuma lógica de negócio nova no handler; apenas dispatch.

## Verificação

- [x] `tests/unit/server/portfolio.get.spec.ts` + `portfolio-payload.contract.spec.ts`
- Smoke manual `GET /api/portfolio` (opcional)
