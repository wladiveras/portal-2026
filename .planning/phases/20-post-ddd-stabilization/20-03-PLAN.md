# 20-03 — Qualidade contínua e backlog técnico opcional

## Objetivo

Fixar gates pós-migração e listar trabalho opcional sem bloquear releases.

## Escopo A (obrigatório leve)

- [x] `.github/workflows/ci.yml` — ordem: `npm ci` → typecheck → unit → build → Playwright chromium → E2E (`CI=true`).
- [x] `TESTING.md` — suítes domain puro (`dashboard-domain-pure.spec.ts`), portfolio (`portfolio-payload.contract.spec.ts`), secção CI.

## Escopo B (opcional / backlog)

- Paridade Drizzle para contextos hoje Supabase-first (`leads`, `access`) — apenas se métricas/custo justificarem.
- Expandir testes de funções puras em `server/domain/dashboard/**` (chart aggregation, burndown edge cases).

## Critérios de aceite

- [x] Backlog opcional isolado na `STATE.md` (secção explícita; não mistura com critérios de release).
- [x] CI referenciado na `STATE.md` (manutenção contínua) e em `TESTING.md`.

## Verificação

- `npm run test` verde após alterações em docs (última execução: ~57 tests).
