---
phase: 08-projects-agile
plan: 3
type: execute
wave: 3
depends_on:
  - 2
files_modified:
  - app/components/dashboard/projects/SprintView.vue
  - app/components/dashboard/projects/BurndownChart.vue
  - server/api/dashboard/projects/[id]/burndown.get.ts
autonomous: true
requirements:
  - DASH-17
must_haves:
  truths:
    - "Sprint view mostra lista de tasks agrupadas por status, capacidade (soma de points), e burndown"
    - "Burndown calcula pontos restantes por dia (cumulativo reverso de tasks `done` com `done_at`)"
---

<objective>
Dar feeling de sprint profissional sem inventar complexidade: burndown simples + agregados.
</objective>

<tasks>
- Adicionar coluna `done_at` em `tasks` (via migration `0006_done_at.sql`).
- Trigger: quando `status` transita para `done`, set `done_at = now()`; quando sai de `done`, clear.
- `BurndownChart.vue`: reusa lib de charts (fase 06).
- `SprintView.vue`: header com range do sprint, progresso (x/y pts), burndown; lista de tasks embaixo.
- `useSprintBurndown(sprintId)` → `{ ideal[], actual[] }`.
</tasks>
