---
phase: 08-projects-agile
plan: 2
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - app/pages/dashboard/projects/index.vue
  - app/pages/dashboard/projects/[id].vue
  - app/components/dashboard/projects/KanbanBoard.vue
  - app/components/dashboard/projects/TaskCard.vue
  - app/components/dashboard/projects/TaskDrawer.vue
  - app/composables/useProject.ts
autonomous: true
requirements:
  - DASH-16
must_haves:
  truths:
    - "Kanban usa 4 colunas (`todo/doing/review/done`) com drag-and-drop (biblioteca: `@vueuse/integrations/useSortable` ou `sortablejs`)"
    - "Drag-and-drop persiste `status` e `position` com update otimista + rollback; conflitos resolvidos por `updated_at`"
    - "`TaskDrawer` permite editar title, description, points, assignee, labels"
---

<objective>
Kanban funcional com DnD; base visual derivada do design da landing (cards em `glass-surface`, labels como chips coloridos).
</objective>

<tasks>
- Instalar `sortablejs` (leve, 11kB) ou preferir `@vueuse/integrations`.
- `KanbanBoard.vue`: 4 colunas; cada coluna tem `useSortable(refColumn, { group: 'tasks', onEnd })`.
- `onEnd`: calcula `position` via `LexoRank`-like (metade entre vizinhos) ou simples `index * 1024`; envia `PATCH /api/dashboard/tasks/[id]` com `{status, position}`.
- `TaskCard.vue`: mostra title, assignee avatar, points, labels (chips).
- `TaskDrawer.vue`: abre por `?task=`; formulário com autosave debounced.
- Listagem de projetos `/dashboard/projects/index.vue` com grid de `ProjectCard` (nome, cor, progresso sprint atual).
</tasks>

<verification>
E2E: arrasta task de `todo` para `doing` e recarrega a página mantendo o status.
</verification>
