---
phase: 05-tracking-infra
plan: 3
type: execute
wave: 3
depends_on:
  - 2
files_modified:
  - app/utils/inPageHashNav.ts
  - app/components/sections/ContactFooterSection.vue
  - app/components/ui/FloatingNav.vue
  - app/composables/useSectionInView.ts
  - e2e/tracking.spec.ts
autonomous: true
requirements:
  - DASH-08
must_haves:
  truths:
    - "Clicking WhatsApp / email / social links in the landing emits `click_*` events with `source=landing.contact`"
    - "Scrolling through each landing section fires exactly one `section_in_view` event per session per section"
    - "Hash clicks via `onInPageHashClick` include the target id in the tracked meta"
---

<objective>
Wire the landing UX to the tracker so we actually receive data. Instrument hash navigation, WhatsApp/email/social CTAs, and section visibility.
</objective>

<tasks>
- Extend `onInPageHashClick` to call `useTracker().trackEvent({ type: 'hash_nav', target })` without awaiting (fire-and-forget).
- `ContactFooterSection.vue`: on WhatsApp click call `trackLead({ source: 'whatsapp', contactValue: landing.contact.whatsappHref })`; on mailto click emit `click_email`; on social links emit `click_social` with `meta.label`.
- `useSectionInView(id)`: composable leveraging `IntersectionObserver`; emits `section_in_view` the first time a section crosses 30% viewport per session (use `sessionStorage`).
- Add `FloatingNav` CTA tracking on open/close for menu usage metrics (optional).
- E2E `e2e/tracking.spec.ts`: intercept `**/api/track/event` and `**/api/track/lead`; assert specific types fire on simulated interactions.
</tasks>

<verification>
`npm run test:e2e` adds the tracking spec and stays green.
</verification>
