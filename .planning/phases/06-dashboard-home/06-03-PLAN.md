---
phase: 06-dashboard-home
plan: 3
type: execute
wave: 2
depends_on:
  - 2
files_modified:
  - package.json
  - app/components/dashboard/charts/LeadsAreaChart.vue
  - app/components/dashboard/charts/SourceDonut.vue
  - app/components/dashboard/charts/FunnelBars.vue
  - server/api/dashboard/charts.get.ts
autonomous: true
requirements:
  - DASH-11
must_haves:
  truths:
    - "Chart lib escolhida entra em `package.json` (recomendado: `@unovis/ts` + `@unovis/vue`)"
    - "Area chart de leads/dia, donut de UTM source, bar chart de funnel de status renderizam responsivo (SSR-safe: renderizam `onMounted`)"
    - "Cores usam tokens CSS (`accent`, `accent-soft`, `muted`); sem hex inline nos componentes"
---

<objective>
Introduzir visualizações. Escolher lib leve e SSR-friendly. Recomendação: **Unovis** (oficial Vue binding, leve, temas via CSS vars).
</objective>

<tasks>
- Instalar `@unovis/ts` e `@unovis/vue`.
- `LeadsAreaChart.vue`: recebe `series: { date, count }[]`; usa `<VisXYContainer>` + `<VisArea>` + `<VisAxis>`. Cor do fill = `accent-soft` com opacidade.
- `SourceDonut.vue`: recebe `slices: { label, value }[]`. Paleta: accent, accent-soft, muted.
- `FunnelBars.vue`: recebe `{ status, value }[]`; barras horizontais com gradiente.
- `server/api/dashboard/charts.get.ts`: retorna `{ leadsByDay, sources, funnel }`.
- Wrapper `BaseChartCard.vue` com `eyebrow`, `title`, `hint` opcional e slot.
</tasks>

<verification>
Montagem em Vitest (happy-dom) para garantir que não quebra em SSR. Smoke visual: `npm run dev` e ver `/dashboard` com dados mockados.
</verification>
