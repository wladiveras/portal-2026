---
phase: 06-dashboard-home
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/dashboard/WelcomeCard.vue
  - app/composables/useLiveClock.ts
  - app/components/dashboard/QuickNotes.vue
  - supabase/migrations/0003_notes.sql
  - tests/unit/composables/useLiveClock.spec.ts
autonomous: true
requirements:
  - DASH-09
must_haves:
  truths:
    - "`useLiveClock()` returns a reactive `Date` updated every 60s (configurable), paused when tab is hidden"
    - "`WelcomeCard` renders saudação correta por faixa horaria na timezone `America/Sao_Paulo` independente do locale do browser"
    - "`QuickNotes` reads/writes `notes` table via Supabase client respecting RLS (`auth.uid() = user_id`)"
---

<objective>
Dar personalidade à home: saudação por faixa horária, relógio vivo e notas pessoais. Tudo alinhado ao design da landing (serif italic, glass surfaces).
</objective>

<interfaces>
```sql
create table public.notes (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  body text not null,
  pinned boolean not null default false,
  created_at timestamptz not null default now()
);
alter table public.notes enable row level security;
create policy notes_self on public.notes for all
  using (user_id = auth.uid())
  with check (user_id = auth.uid());
```

`useLiveClock({ intervalMs = 30_000, timezone = 'America/Sao_Paulo' })` → `{ now, greeting, formatted }` (greeting: `bom dia` 05-11:59, `boa tarde` 12-17:59, `boa noite` 18-04:59).
</interfaces>

<tasks>
- Migration `0003_notes.sql` com tabela e RLS por `auth.uid()`.
- `useLiveClock.ts` com timer baseado em `setInterval` + `visibilitychange` pausa/retoma.
- `WelcomeCard.vue`: grid 2 colunas (texto à esquerda, clock/date à direita); usa `font-display italic` para saudação, `accent-gradient` no horário.
- `QuickNotes.vue`: lista paginada, input para nova nota, toggle pinned, delete com confirm. Estados `loading`, `error`.
- Unit tests: `useLiveClock` (mock `Date`, intervalo, greeting ranges).
</tasks>

<verification>
`tests/unit/composables/useLiveClock.spec.ts` cobre pelo menos 05h00, 11h59, 12h00, 17h59, 18h00, 04h59 → greeting esperado.
</verification>
