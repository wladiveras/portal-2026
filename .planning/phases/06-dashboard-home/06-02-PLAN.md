---
phase: 06-dashboard-home
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/dashboard/StatCard.vue
  - app/components/dashboard/StatGrid.vue
  - server/api/dashboard/stats.get.ts
  - app/composables/useDashboardStats.ts
autonomous: true
requirements:
  - DASH-10
must_haves:
  truths:
    - "StatCard mostra label, valor principal em `font-display italic` com `accent-gradient bg-clip-text`, delta vs periodo anterior e ícone"
    - "`GET /api/dashboard/stats?range=7d|30d|90d` agrega `leads`, `events`, `projects`, `tasks` numa unica resposta"
    - "Componente lida com loading skeleton e estado `no-data` (sem lançar erros visuais)"
---

<objective>
Entregar o "ribbon de métricas" no topo da home. Dados reais quando fase 05 popular; skeleton e fallback até lá.
</objective>

<tasks>
- `StatCard.vue`: props `{ label, value, suffix?, delta?, icon }`. Aplica `glass-surface` + borda sutil.
- `StatGrid.vue`: agrupa 4 cards (`leads_7d`, `conversions_7d`, `active_projects`, `open_tasks`).
- Server endpoint `/api/dashboard/stats.get.ts`: usa service role para contagens agregadas; resposta tipada.
- `useDashboardStats(range)`: `useFetch` com `key` no range; revalida a cada 60s (`refreshInterval`).
- Skeleton: barras animadas respeitando `prefers-reduced-motion`.
</tasks>

<verification>
Unit: `StatCard` snapshot + delta formatting (positivo/negativo). Integration: mock `useFetch` resposta e assert dos valores nos cards.
</verification>
