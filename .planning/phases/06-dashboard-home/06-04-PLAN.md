---
phase: 06-dashboard-home
plan: 4
type: execute
wave: 2
depends_on:
  - 2
files_modified:
  - app/components/dashboard/ActivityFeed.vue
  - server/api/dashboard/activity.get.ts
  - app/pages/dashboard/index.vue
autonomous: true
requirements:
  - DASH-09
  - DASH-10
  - DASH-11
must_haves:
  truths:
    - "Feed mostra últimos 20 `lead_events` + ultimas 5 notas, ordenados por timestamp desc"
    - "Paginação cursor-based (`created_at` + `id`)"
    - "Pagina `/dashboard/index.vue` compõe todos os blocos num layout grid 12 col"
---

<objective>
Amarrar a home: `WelcomeCard` + `StatGrid` + charts + `ActivityFeed` + `QuickNotes` numa página polida.
</objective>

<tasks>
- `ActivityFeed.vue`: renderiza timeline com ícones por tipo de evento; usa `useIntersectionObserver` para infinite scroll (`@vueuse/core`).
- `/api/dashboard/activity`: query `lead_events` join `leads` + `notes`.
- Rescrever `app/pages/dashboard/index.vue`:
  - linha 1: WelcomeCard (col-span-8) + clock (col-span-4).
  - linha 2: StatGrid (col-span-12).
  - linha 3: LeadsAreaChart (col-span-8) + SourceDonut (col-span-4).
  - linha 4: FunnelBars (col-span-6) + QuickNotes (col-span-3) + ActivityFeed (col-span-3).
- Responsivo: stack em mobile com gutters de 16px.
</tasks>

<verification>
E2E `e2e/dashboard-home.spec.ts`: login admin, visita `/dashboard`, assert presença de headings, cards e pelo menos um chart.
</verification>
