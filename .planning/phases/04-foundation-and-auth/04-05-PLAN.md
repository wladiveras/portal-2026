---
phase: 04-foundation-and-auth
plan: 5
type: execute
wave: 3
depends_on:
  - 2
  - 4
files_modified:
  - app/layouts/dashboard.vue
  - app/pages/login.vue
  - app/pages/confirm.vue
  - app/pages/dashboard/index.vue
  - app/components/dashboard/SidebarNav.vue
  - app/components/dashboard/TopBar.vue
  - app/assets/css/main.css
autonomous: true
requirements:
  - DASH-05
must_haves:
  truths:
    - "Layout `dashboard.vue` renders Sidebar + TopBar + `<slot />` and uses design tokens (glass, serif display for titles)"
    - "`/login` supports magic link AND email+password; shows inline errors; respects `?redirect=`"
    - "`[data-theme='dark']` block in main.css defines dark palette; switching theme does not affect landing when on `/`"
---

<objective>
Deliver the visual shell that every dashboard page will inherit. The shell reuses landing tokens (Inter body / Instrument Serif display / `accent-gradient` / `glass-surface`) and introduces a dark token set opt-in via a `data-theme` attribute.
</objective>

<context>
@app/assets/css/main.css
@tailwind.config.ts
@app/app.vue

Identity constraints:
- Section titles use `font-display italic` with color `text-text-primary/88`.
- Buttons reuse `GradientRingButton` where possible.
- Surfaces prefer `glass-surface` with subtle borders.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard layout</name>
  <files>app/layouts/dashboard.vue,app/components/dashboard/SidebarNav.vue,app/components/dashboard/TopBar.vue</files>
  <action>Layout grid: `grid-cols-[240px_1fr]` on md+, stack on mobile. `SidebarNav` items: Overview, Leads, Projects, Access (only admin). `TopBar` with page title slot, theme toggle, and user pill (avatar initials + role chip, logout button). Keep glass surfaces, `font-display` italic for section headings.</action>
</task>

<task type="auto">
  <name>Task 2: Login page</name>
  <files>app/pages/login.vue,app/pages/confirm.vue</files>
  <action>`/login` with two modes: magic link (email) and password (email+password). Use `useSupabaseClient()` + `signInWithOtp` or `signInWithPassword`. After success, `navigateTo(redirect ?? '/dashboard')`. `/confirm` simply shows "Verificando…" and completes magic-link callback; Nuxt Supabase module handles token exchange.</action>
</task>

<task type="auto">
  <name>Task 3: Dashboard index placeholder</name>
  <files>app/pages/dashboard/index.vue</files>
  <action>Minimal page with `definePageMeta({ layout: 'dashboard' })` and the welcome heading placeholder; fase 06 substitui o conteudo.</action>
</task>

<task type="auto">
  <name>Task 4: Dark tokens</name>
  <files>app/assets/css/main.css</files>
  <action>Add block:
```css
[data-theme='dark'] {
  --bg: 222 27% 7%;
  --surface: 222 22% 11%;
  --text: 210 20% 96%;
  --muted: 215 14% 65%;
  --stroke: 222 18% 18%;
}
```
Ensure `body` continues to read from HSL vars; no change to the landing root element (which does not set `data-theme`).</action>
</task>

</tasks>

<verification>
E2E `tests/e2e/dashboard-shell.spec.ts`: visit `/dashboard` without session, expect URL `/login?redirect=/dashboard`; from `/login` a mocked magic-link flow lands on `/dashboard` rendering the welcome heading.
</verification>

<success_criteria>
User can open `/login`, authenticate, land on `/dashboard` with layout, toggle theme, and log out. Landing `/` unchanged visually.
</success_criteria>
