---
phase: 04-foundation-and-auth
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - nuxt.config.ts
  - .env.example
autonomous: true
requirements:
  - DASH-02
user_setup:
  - service: Supabase
    why: "Provides Auth + Postgres + RLS for the dashboard"
    env_vars:
      - SUPABASE_URL
      - SUPABASE_KEY
      - SUPABASE_SERVICE_KEY
    dashboard_config:
      - task: "Create a new Supabase project (free tier ok)"
        location: "https://supabase.com/dashboard"
      - task: "Copy Project URL, anon key, service_role key into .env (never commit service key)"
        location: "Project Settings → API"
must_haves:
  truths:
    - "`@nuxtjs/supabase` is installed and listed in `modules` of nuxt.config.ts"
    - "`redirectOptions` scope the auto-redirect to `/dashboard/**` so the landing / stays public"
    - "`SUPABASE_SERVICE_KEY` is referenced from `runtimeConfig` (server-only) and never leaks to client"
  artifacts:
    - path: .env.example
      provides: "Template of required env vars for the dashboard"
      contains: 'SUPABASE_URL'
    - path: nuxt.config.ts
      provides: "Supabase module wired with route-scoped redirect"
      contains: '@nuxtjs/supabase'
---

<objective>
Install and configure the official `@nuxtjs/supabase` module without breaking the landing. Ensure `SUPABASE_SERVICE_KEY` is reachable only from server-side code.
</objective>

<context>
@nuxt.config.ts
@.planning/codebase/ARCHITECTURE.md
Landing is at `/`; dashboard lives under `/dashboard/**` (phase 04-05). Redirect must not intercept the landing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install module</name>
  <files>package.json</files>
  <action>`npm i @nuxtjs/supabase`.</action>
  <verify><automated>node -e "require('@nuxtjs/supabase/package.json')"</automated></verify>
  <done>Module is resolvable from `node_modules`.</done>
</task>

<task type="auto">
  <name>Task 2: Configure module in nuxt.config</name>
  <files>nuxt.config.ts</files>
  <action>Add `@nuxtjs/supabase` to `modules`. Add `supabase` block with `redirectOptions: { login: '/login', callback: '/confirm', include: ['/dashboard/**'], exclude: ['/'] }`. Add `runtimeConfig.supabaseServiceKey` reading `SUPABASE_SERVICE_KEY` (server-only).</action>
  <verify><automated>npm run build</automated></verify>
  <done>Build succeeds; dev server boots without intercepting `/`.</done>
</task>

<task type="auto">
  <name>Task 3: .env.example</name>
  <files>.env.example</files>
  <action>Create/update `.env.example` with `SUPABASE_URL=`, `SUPABASE_KEY=`, `SUPABASE_SERVICE_KEY=`, `PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000`. Never commit actual secrets.</action>
  <verify><automated>grep -c SUPABASE_URL .env.example</automated></verify>
  <done>Template file documents every env var consumed by the app.</done>
</task>

</tasks>

<verification>
`npm run build` works with empty `.env` (module is lazy); dev server loads `/` without redirect.
</verification>

<success_criteria>
Supabase integration point is stable; subsequent plans can use `useSupabaseUser`, `useSupabaseClient`, and server-side `serverSupabaseServiceRole`.
</success_criteria>
