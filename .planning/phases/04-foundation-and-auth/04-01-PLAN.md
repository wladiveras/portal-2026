---
phase: 04-foundation-and-auth
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - DASH-01
user_setup: []
must_haves:
  truths:
    - "`vue-tsc` and `@vue/language-core` are in devDependencies so `npm run typecheck` does not depend on npx cache"
    - "`npm run typecheck` exits 0 without the Volar / language-core MODULE_NOT_FOUND warning"
  artifacts:
    - path: package.json
      provides: "vue-tsc + @vue/language-core pinned in devDependencies"
      contains: 'vue-tsc'
---

<objective>
Fix the TypeScript tooling so `npm run typecheck` is self-contained and green. The current run falls back to `npx vue-tsc@3.2.7` which cannot resolve `@vue/language-core` from its cached install, producing a MODULE_NOT_FOUND warning that blocks CI reliability.
</objective>

<context>
@package.json
@.planning/codebase/TESTING.md
Current log (phase 03):
```
[Vue] Resolve plugin path failed: vue-router/volar/sfc-route-blocks
Error: Cannot find module '@vue/language-core'
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin vue-tsc and @vue/language-core</name>
  <files>package.json</files>
  <action>`npm i -D vue-tsc@^3 @vue/language-core@^3`. Commit the updated lockfile.</action>
  <verify><automated>npm run typecheck</automated></verify>
  <done>Typecheck runs without the npx bootstrap warning and exits 0. If legitimate TS errors surface in the codebase, fix only trivial ones; document the rest as follow-ups in SUMMARY.</done>
</task>

</tasks>

<verification>
`npm run typecheck` succeeds; diff touches only `package.json` and `package-lock.json`.
</verification>

<success_criteria>
No reliance on `npx` cache for the typecheck script.
</success_criteria>

<output>
After completion, append a short note to `.planning/phases/04-foundation-and-auth/04-01-SUMMARY.md`.
</output>
