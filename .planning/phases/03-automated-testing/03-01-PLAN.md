---
phase: 03-automated-testing
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - nuxt.config.ts
  - vitest.config.ts
autonomous: true
requirements:
  - TEST-01
user_setup: []
must_haves:
  truths:
    - "Running `npm run test` executes Vitest and exits 0 with at least one passing smoke test"
    - "Running `npm run typecheck` runs Nuxt official typecheck and exits 0"
  artifacts:
    - path: vitest.config.ts
      provides: "Vitest project config wired to Nuxt 4"
      min_lines: 15
    - path: package.json
      provides: "Scripts test, typecheck, and devDependencies for testing"
      contains: '"test"'
  key_links:
    - from: vitest.config.ts
      to: nuxt.config.ts
      via: "@nuxt/test-utils / Nuxt-Vite integration"
      pattern: "defineVitestConfig|vitest"
---

<objective>
Bootstrap **Vitest** with **@nuxt/test-utils** and **@vue/test-utils** for Nuxt 4.4, add **`nuxi typecheck`** as an npm script, and commit one **smoke test** that proves the harness runs (e.g. trivial `expect(true)` in `tests/unit/smoke.spec.ts` or a minimal composable import).

Purpose: Unblocks all other plans; aligns with current stack (Vue 3.5, Pinia, Nitro).

Output: `vitest.config.ts`, updated `package.json` / `nuxt.config.ts`, passing `npm run test` and `npm run typecheck`.

**Assumption:** `.planning/ROADMAP.md` did not exist; this is **Phase 03**. **PR slice:** merge this plan alone or with `03-02` as first PR after review.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/codebase/TESTING.md
@.planning/codebase/STACK.md
@package.json
@nuxt.config.ts

Consult current [Nuxt testing documentation](https://nuxt.com/docs/getting-started/testing) for Nuxt 4 + Vitest (`@nuxt/test-utils` setup, `environment: 'nuxt'`, happy-dom or jsdom).

<interfaces>
From `package.json` (extend, do not remove existing scripts):
```json
{
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "nuxt typecheck"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add devDependencies and npm scripts</name>
  <files>package.json</files>
  <action>Add devDependencies: `vitest`, `@nuxt/test-utils`, `@vue/test-utils`, `happy-dom` (or `jsdom` per Nuxt doc recommendation for Nuxt 4.4). Add scripts: `test` → `vitest run`, `test:watch` → `vitest`, `typecheck` → `nuxt typecheck`. Run `npm install`. Do not add Playwright in this plan (plan 05). Prefer versions compatible with `nuxt@^4.4.2`.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>`npm run typecheck` completes with exit code 0 (fix any surfaced TS issues only if trivial and blocking; otherwise document in SUMMARY).</done>
</task>

<task type="auto">
  <name>Task 2: Vitest + Nuxt config and smoke test</name>
  <files>vitest.config.ts,nuxt.config.ts,tests/unit/smoke.spec.ts</files>
  <action>Create `vitest.config.ts` using the pattern from Nuxt docs for Vitest + Nuxt (e.g. `defineVitestConfig` from `@nuxt/test-utils/config` or equivalent current API). Ensure tests can resolve `~/` aliases. Add minimal `tests/unit/smoke.spec.ts` that runs in Nuxt/Vitest environment (`expect(1 + 1).toBe(2)` is enough). Touch `nuxt.config.ts` only if the official setup requires a module or `vite.test` block.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>`npm run test` exits 0; smoke spec runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| devDependencies | New packages from npm registry — supply-chain risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | npm packages | mitigate | Use lockfile commit; pin major versions; `npm audit` in CI (plan 06) |
| T-03-02 | Information disclosure | test logs | accept | No secrets in repo tests; smoke test is trivial |
</threat_model>

<verification>
`npm run test` and `npm run typecheck` both pass locally.
</verification>

<success_criteria>
Harness is the single source for unit runs; typecheck is scriptable for CI.
</success_criteria>

<output>
After completion, create `.planning/phases/03-automated-testing/03-01-SUMMARY.md`
</output>
