---
phase: 03-automated-testing
plan: 3
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - tests/unit/server/portfolio.get.spec.ts
autonomous: true
requirements:
  - TEST-03
user_setup: []
must_haves:
  truths:
    - "Request to `/api/portfolio` returns status 200"
    - "JSON body includes keys: careerStartYear, about, skills, experience, projects, testimonials"
    - "`about` object includes headline, title, summary, longText, profileImage"
  artifacts:
    - path: tests/unit/server/portfolio.get.spec.ts
      provides: "Nitro route smoke test"
      min_lines: 40
  key_links:
    - from: tests/unit/server/portfolio.get.spec.ts
      to: server/api/portfolio.get.ts
      via: "Nuxt test server $fetch or equivalent"
      pattern: "/api/portfolio"
---

<objective>
Smoke-test the **Nitro handler** `server/api/portfolio.get.ts`: **200** response and **required keys** present on the JSON body matching `PortfolioData` (structural smoke, not full content equality).

Purpose: Catches accidental handler breakage or wrong export shape.

Output: `tests/unit/server/portfolio.get.spec.ts`.

**PR slice:** Can ship in parallel with plans 02 and 04 after plan 01 (same wave).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@server/api/portfolio.get.ts
@app/types/portfolio.ts
@.planning/codebase/ARCHITECTURE.md

Use `@nuxt/test-utils` **server** helpers (e.g. `setup()`, then `$fetch('/api/portfolio')` or documented `fetch` pattern for Nuxt 4). Alternative acceptable if docs recommend: `supertest`-style wrapper — prefer official Nuxt/Nitro pattern first.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nitro GET /api/portfolio smoke test</name>
  <files>tests/unit/server/portfolio.get.spec.ts</files>
  <action>Create a test that starts the Nuxt test server (per @nuxt/test-utils), calls `GET /api/portfolio`, asserts status 200. Parse JSON and assert presence of top-level keys: `careerStartYear`, `about`, `skills`, `experience`, `projects`, `testimonials`. Assert `about` has `headline`, `title`, `summary`, `longText`, `profileImage`. Use `expect(Array.isArray(...))` for array fields. Avoid asserting full string copy (content drifts).</action>
  <verify>
    <automated>npm run test -- tests/unit/server/portfolio.get.spec.ts</automated>
  </verify>
  <done>Spec passes; runtime uses real handler code path.</done>
</task>

<task type="auto">
  <name>Task 2: Keep server test time bounded</name>
  <files>tests/unit/server/portfolio.get.spec.ts</files>
  <action>Reuse Nuxt test setup with `setupFiles` or `beforeAll`/`afterAll` per docs so the server starts once per file (not per test) when supported. If the framework spins per test, document runtime in SUMMARY.</action>
  <verify>
    <automated>npm run test -- tests/unit/server/portfolio.get.spec.ts</automated>
  </verify>
  <done>No redundant flaky patterns; teardown documented if slow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local test server | Only localhost in test process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information disclosure | API returns public portfolio JSON | accept | Same data as production static handler; no secrets |
| T-03-06 | Tampering | test invokes real handler | accept | Smoke read-only; no mutation |
</threat_model>

<verification>
`npm run test -- tests/unit/server/portfolio.get.spec.ts` passes under 120s locally (adjust if cold build requires more once, document).
</verification>

<success_criteria>
Required keys assertion fails if handler shape regresses.
</success_criteria>

<output>
After completion, create `.planning/phases/03-automated-testing/03-03-SUMMARY.md`
</output>
