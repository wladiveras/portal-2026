---
phase: 03-automated-testing
plan: 5
type: execute
wave: 3
depends_on:
  - 1
files_modified:
  - package.json
  - playwright.config.ts
  - e2e/landing.spec.ts
autonomous: true
requirements:
  - TEST-05
user_setup: []
must_haves:
  truths:
    - "After load, `#home` (or main hero landmark) and `#work` and `#contact-cta` are visible"
    - "Opening `FloatingNav` and activating Projetos / Fale comigo moves focus or scroll target to `#work` / `#contact-cta` (hash or element in view)"
    - "WhatsApp primary CTA `href` equals `landing.contact.whatsappHref` from `app/data/landing.ts`"
  artifacts:
    - path: playwright.config.ts
      provides: "Playwright baseURL + webServer dev"
      min_lines: 25
    - path: e2e/landing.spec.ts
      provides: "Landing critical path spec"
      min_lines: 55
  key_links:
    - from: e2e/landing.spec.ts
      to: app/pages/index.vue
      via: "browser navigation to /"
      pattern: "FloatingNav|#work|contact-cta"
---

<objective>
Add **Playwright** E2E tests for the single-page landing: **loading → main content**, **FloatingNav** in-page navigation to **`#work`** and **`#contact-cta`**, and **WhatsApp** link `href` synced with **`landing.contact.whatsappHref`**.

Purpose: Guards regressions called out in `TESTING.md` / `CONCERNS.md` (loading handoff, hash nav, contact links).

Output: `playwright.config.ts`, `e2e/landing.spec.ts`, npm script `test:e2e`.

**PR slice:** Second PR after unit-test PR, or combined if scope acceptable.

**UAT (manual, video-heavy):** After automated E2E passes, spot-check once in a real browser: loading diamond/hero video transition, external CTA video in contact (`landing.contact.ctaVideoSrc`), and smooth scroll feel — **not** fully automatable.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/pages/index.vue
@app/components/ui/FloatingNav.vue
@app/components/sections/LoadingScreen.vue
@app/data/landing.ts
@.planning/codebase/CONCERNS.md

<interfaces>
`landing.nav.items` includes `{ id: 'work', label: 'Projetos', href: '#work' }`.
`landing.nav.cta` is `{ label: 'Fale comigo', href: '#contact-cta', ... }`.
`landing.contact.ctaButtonLabel` is `'Chamar no WhatsApp'`; `whatsappHref` is the expected `wa.me` URL.

DOM anchors: `HeroSection` uses `id="home"`; `SelectedWorksSection` uses `id="work"`; `#contact-cta` is on the CTA card in `ContactFooterSection.vue`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright and config</name>
  <files>package.json,playwright.config.ts</files>
  <action>Add `@playwright/test` devDependency; `npx playwright install` with `--with-deps` only in CI (document). Create `playwright.config.ts` with `baseURL` `http://127.0.0.1:3000`, `webServer: { command: 'npm run dev', url: baseURL, timeout: 120_000, reuseExistingServer: !process.env.CI }`. Set **reduced motion** at browser context level (`reducedMotion: 'reduce'`) to shorten `LoadingScreen` path (RAF-based completion) while still asserting unmount + sections. Add script `test:e2e`: `playwright test`.</action>
  <verify>
    <automated>npx playwright test --list</automated>
  </verify>
  <done>Config loads; Playwright discovers tests (0 files ok before task 2).</done>
</task>

<task type="auto">
  <name>Task 2: landing.spec.ts critical flows</name>
  <files>e2e/landing.spec.ts</files>
  <action>Import `landing` from `../app/data/landing` (adjust relative path from `e2e/`). Implement tests: (1) `page.goto('/')` — wait until `#home` is visible (implies `LoadingScreen` completed and hero mounted); assert `#work` exists in DOM. (2) Click nav toggle `getByRole('button', { name: /Abrir menu/i })`, then `getByRole('link', { name: 'Projetos' })` (sr-only label); assert `page.url()` ends with `#work` OR `locator('#work').scrollIntoViewIfNeeded` then is in viewport. (3) Open menu again, click `getByRole('link', { name: 'Fale comigo' })`, assert `#contact-cta` targeted similarly. (4) `getByRole('link', { name: landing.contact.ctaButtonLabel })` assert `getAttribute('href') === landing.contact.whatsappHref`. Use reasonable timeouts (45–90s) for first load in CI.</action>
  <verify>
    <automated>npm run test:e2e</automated>
  </verify>
  <done>All E2E tests pass locally with dev server auto-started.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ localhost | E2E drives local dev server only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Spoofing | E2E against dev server | accept | CI runs on trusted runner |
| T-03-10 | Information disclosure | Public portfolio URLs in tests | accept | Same as production marketing content |
</threat_model>

<verification>
`npm run test:e2e` passes with `CI=true` optional check in SUMMARY.
</verification>

<success_criteria>
Automated coverage for loading handoff, hash nav, and WhatsApp href SSOT from `landing.ts`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-automated-testing/03-05-SUMMARY.md`
</output>
