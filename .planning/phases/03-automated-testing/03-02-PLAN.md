---
phase: 03-automated-testing
plan: 2
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - tests/unit/stores/portfolio.store.spec.ts
  - app/stores/portfolio.ts
autonomous: true
requirements:
  - TEST-02
user_setup: []
must_haves:
  truths:
    - "Two overlapping `fetchPortfolioData()` calls share one in-flight promise and `$fetch` is invoked once"
    - "Successful response assigns `careerStartYear`, `about`, `skills`, `experience`, `projects`, `testimonials` and ends with `loading === false`, `error === null`"
    - "Rejected `$fetch` sets `error` to a non-empty string and `loading === false`"
  artifacts:
    - path: tests/unit/stores/portfolio.store.spec.ts
      provides: "Pinia store tests for fetchPortfolioData"
      min_lines: 80
  key_links:
    - from: tests/unit/stores/portfolio.store.spec.ts
      to: app/stores/portfolio.ts
      via: "mock $fetch + createPinia + store"
      pattern: "fetchPortfolioData|\\$fetch"
---

<objective>
Add **focused unit tests** for `usePortfolioStore` action `fetchPortfolioData` matching **current** `app/stores/portfolio.ts` behavior: module-level deduplication, success merge, and error handling.

Purpose: Protects the highest-risk client data path (`portfolio.client.ts` → store).

Output: `tests/unit/stores/portfolio.store.spec.ts`; optional minimal **test-only reset** in `app/stores/portfolio.ts` if module singleton `portfolioFetchInFlight` cannot be isolated otherwise (prefer `vi.resetModules()` + dynamic import first; only export a reset if strictly necessary and tree-shaken from production).

**PR slice:** Pair with `03-01` in first PR, or follow immediately after harness PR.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/stores/portfolio.ts
@app/types/portfolio.ts
@.planning/codebase/ARCHITECTURE.md

<interfaces>
From `app/types/portfolio.ts`:
```typescript
export interface PortfolioData {
  careerStartYear: number
  about: About
  skills: string[]
  experience: Experience[]
  projects: Project[]
  testimonials: Testimonial[]
}
```

Store state extends `PortfolioData` with `loading: boolean` and `error: string | null`.

`fetchPortfolioData` uses global `$fetch<PortfolioData>('/api/portfolio')`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests — deduplication and success merge</name>
  <files>tests/unit/stores/portfolio.store.spec.ts</files>
  <behavior>
    - When `fetchPortfolioData` is called twice concurrently while `$fetch` is unresolved, `$fetch` is called exactly once and both callers await the same completion.
    - On resolve, store fields match the mocked payload shape; `loading` ends `false`; `error` is `null`.
  </behavior>
  <action>Implement with Vitest + Pinia `createPinia` + `setActivePinia`. Mock `$fetch` with `vi.fn()` returning a delayed Promise for the dedup case, then a full `PortfolioData`-shaped object for success. Use `@vue/test-utils` or plain store access via `usePortfolioStore()` inside `setup` if needed. If module-level `portfolioFetchInFlight` survives across tests, use `vi.resetModules()` and dynamic `import('~/stores/portfolio')` per test file section, or add a guarded `resetPortfolioFetchStateForTests()` exported only under vitest (document in SUMMARY).</action>
  <verify>
    <automated>npm run test -- tests/unit/stores/portfolio.store.spec.ts</automated>
  </verify>
  <done>Dedup and success assertions pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tests — error path</name>
  <files>tests/unit/stores/portfolio.store.spec.ts,app/stores/portfolio.ts</files>
  <behavior>
    - When `$fetch` rejects with `Error('network')`, store `error` equals `'network'` (or non-empty message) and `loading === false`.
  </behavior>
  <action>Mock `$fetch` to reject. Assert `console.error` may fire (optional spy). Only modify `app/stores/portfolio.ts` if required for test isolation (minimal diff).</action>
  <verify>
    <automated>npm run test -- tests/unit/stores/portfolio.store.spec.ts</automated>
  </verify>
  <done>Error path test passes; production behavior unchanged except allowed test hook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mock $fetch | Tests do not hit real network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Elevation of privilege | test-only export on store | mitigate | If adding reset helper, guard with `import.meta.vitest` or omit from bundle; document |
| T-03-04 | Denial of service | N/A in unit test | accept | — |
</threat_model>

<verification>
`npm run test -- tests/unit/stores/portfolio.store.spec.ts` passes.
</verification>

<success_criteria>
All three behaviors (dedup, success, error) are asserted explicitly against store state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-automated-testing/03-02-SUMMARY.md`
</output>
