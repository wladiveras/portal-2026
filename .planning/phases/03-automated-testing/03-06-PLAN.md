---
phase: 03-automated-testing
plan: 6
type: execute
wave: 4
depends_on:
  - 1
  - 2
  - 3
  - 4
  - 5
files_modified:
  - .github/workflows/ci.yml
autonomous: false
requirements:
  - TEST-01
  - TEST-02
  - TEST-03
  - TEST-04
  - TEST-05
  - TEST-06
user_setup:
  - service: GitHub Actions
    why: "CI runs on push/PR when workflow file is present"
    env_vars: []
    dashboard_config:
      - task: "Ensure Actions enabled for repository"
        location: "GitHub repo Settings → Actions"
must_haves:
  truths:
    - "CI workflow runs typecheck, unit tests, production build, Playwright E2E (with browser install)"
    - "A human confirms video/motion spot-check once per release or after media changes"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "CI pipeline"
      min_lines: 35
  key_links:
    - from: .github/workflows/ci.yml
      to: package.json
      via: "npm run typecheck, test, build, test:e2e"
      pattern: "npm run"
---

<objective>
Add **GitHub Actions** workflow that runs **`npm run typecheck`**, **`npm run test`**, **`nuxt build`**, and **`npm run test:e2e`** (with Playwright browser installation). Close the phase with a **manual UAT** checkpoint for **video-heavy** UI called out in `TEST-06`.

Purpose: Replaces “no CI” gap from `TESTING.md`; keeps motion/media verification human where automation is brittle.

Output: `.github/workflows/ci.yml` (name e.g. `ci.yml`).

**Assumption:** Phase **03**; no prior `STATE.md`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@package.json
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>Create workflow on `push` and `pull_request` to default branch. Job: checkout, setup Node with version matching project (20.x LTS), cache npm, `npm ci`, `npm run typecheck`, `npm run test`, `npm run build`, `npx playwright install --with-deps` (Linux), `npm run test:e2e` with `CI: true`. Set adequate timeouts for Nuxt build + first Playwright run. Fail fast on first step failure.</action>
  <verify>
    <automated>npm run typecheck && npm run test</automated>
  </verify>
  <done>Workflow file includes steps for `typecheck`, `test`, `build`, Playwright install, and `test:e2e`; YAML is syntactically valid (optional: run `actionlint` if installed).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full automated suite (typecheck, unit, build, E2E) wired in CI; Playwright uses reduced motion for speed.</what-built>
  <how-to-verify>
    1. Open a PR or push to trigger the workflow; confirm all jobs green.
    2. **Manual spot-check (video-heavy):** run `npm run dev`, watch **loading → hero** handoff with real video assets; open **contact** section and confirm **CTA preview video** loads (`landing.contact.ctaVideoSrc`); toggle **FloatingNav** and scroll sections without jank.
    3. Optional: test with **prefers-reduced-motion: off** in OS to confirm non-reduced path still acceptable.
  </how-to-verify>
  <resume-signal>Type **approved** or list issues (asset missing, flaky E2E timeout, etc.).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub runner | Untrusted fork PRs — limit secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Elevation of privilege | fork PR workflows | mitigate | Use `pull_request` (not `pull_request_target`) without secrets; read-only CI |
| T-03-12 | Tampering | cached node_modules | accept | npm ci verifies lockfile |
</threat_model>

<verification>
Workflow file present; optional `gh workflow run` if authenticated.
</verification>

<success_criteria>
CI green on clean tree; human sign-off recorded for media/motion spot-check.
</success_criteria>

<output>
After completion, create `.planning/phases/03-automated-testing/03-06-SUMMARY.md`
</output>
