---
phase: 03-automated-testing
plan: 4
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - tests/unit/composables/useReducedMotion.spec.ts
  - tests/unit/composables/useGsapCleanup.spec.ts
autonomous: true
requirements:
  - TEST-04
user_setup: []
must_haves:
  truths:
    - "`useReducedMotion` sets `prefersReducedMotion` from `matchMedia('(prefers-reduced-motion: reduce)')` after mount and updates on `change`"
    - "`useGsapCleanup` registers tweens/triggers and `killAll` kills them and clears registry; unmount triggers cleanup"
  artifacts:
    - path: tests/unit/composables/useReducedMotion.spec.ts
      provides: "Reduced motion composable tests"
      min_lines: 40
    - path: tests/unit/composables/useGsapCleanup.spec.ts
      provides: "GSAP cleanup composable tests"
      min_lines: 40
  key_links:
    - from: tests/unit/composables/useReducedMotion.spec.ts
      to: app/composables/useReducedMotion.ts
      via: "mount stub component"
      pattern: "matchMedia"
---

<objective>
Test **high-value composables** without full DOM/video: `useReducedMotion` (media query + listener) and `useGsapCleanup` (registry + kill + unmount).

Purpose: Regression safety for motion accessibility and GSAP leak prevention per `CONCERNS.md`.

Output: Two spec files under `tests/unit/composables/`.

**PR slice:** Same PR as other unit tests after harness.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@app/composables/useReducedMotion.ts
@app/composables/useGsapCleanup.ts
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: useReducedMotion with matchMedia mock</name>
  <files>tests/unit/composables/useReducedMotion.spec.ts</files>
  <action>Stub `window.matchMedia` to return a controllable `MediaQueryList` (matches true/false, `addEventListener`/`removeEventListener` capture callbacks). Mount a tiny SFC or `defineComponent` that calls `useReducedMotion()`, assert `prefersReducedMotion` after `await nextTick()`. Dispatch `change` to flip `matches`; assert ref updates. Verify listener removed on unmount (spy on `removeEventListener`).</action>
  <verify>
    <automated>npm run test -- tests/unit/composables/useReducedMotion.spec.ts</automated>
  </verify>
  <done>Mount, update, unmount behaviors asserted.</done>
</task>

<task type="auto">
  <name>Task 2: useGsapCleanup with mocked GSAP objects</name>
  <files>tests/unit/composables/useGsapCleanup.spec.ts</files>
  <action>Do **not** require real ScrollTrigger registration. Create fake objects: `{ kill: vi.fn(), scrollTrigger: { kill: vi.fn() } }` mimicking tween with scrollTrigger, and `{ kill: vi.fn() }` for simple trigger. Mount component using `useGsapCleanup()`, call `add()` with fakes, call `killAll()` and assert `kill`/`scrollTrigger.kill` invoked and second `killAll` is safe. Unmount wrapper and assert kills ran via `onUnmounted` (if not already cleared, assert no double-kill errors).</action>
  <verify>
    <automated>npm run test -- tests/unit/composables/useGsapCleanup.spec.ts</automated>
  </verify>
  <done>Kill paths covered for tween+scrollTrigger and plain trigger-like object.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mocked browser APIs | Tests validate composable logic only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Spoofing | matchMedia mock | accept | Test-only; production uses real UA preference |
| T-03-08 | Denial of service | N/A | accept | — |
</threat_model>

<verification>
Both composable spec files pass in isolation and full `npm run test`.
</verification>

<success_criteria>
Composables behavior locked without relying on hero/loading video assets.
</success_criteria>

<output>
After completion, create `.planning/phases/03-automated-testing/03-04-SUMMARY.md`
</output>
