---
phase: 09-access-management
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/dashboard/access/index.vue
  - app/components/dashboard/access/ProfilesTable.vue
  - server/api/dashboard/profiles.get.ts
  - server/api/dashboard/profiles/[id].patch.ts
autonomous: true
requirements:
  - DASH-18
must_haves:
  truths:
    - "Pagina `/dashboard/access` só é acessível para role `admin` (middleware `role`)"
    - "ProfilesTable lista todos os profiles com colunas: name, email (de auth.users), role, ultimo login, status, acoes"
    - "Admin pode alterar role e toggle disabled; não consegue remover-se a si próprio (UI e servidor)"
---

<objective>
Admin vê todos os utilizadores, altera roles e desativa acessos em um ecrã só.
</objective>

<tasks>
- `ProfilesTable.vue`: colunas sortable, filtros rápidos (role chip), estado empty.
- `GET /api/dashboard/profiles`: admin join `auth.users` (via service role) → retorna `{ id, email, last_sign_in_at, role, disabled, full_name }`.
- `PATCH /api/dashboard/profiles/[id]`: valida self-edit (`id != auth.uid()` para mudar role/disabled); admin only.
- Log no `audit_log` (stub até plano 09-03 criar tabela real).
</tasks>
