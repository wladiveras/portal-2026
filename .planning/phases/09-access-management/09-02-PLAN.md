---
phase: 09-access-management
plan: 2
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - app/components/dashboard/access/InviteUserForm.vue
  - server/api/dashboard/invites.post.ts
  - server/api/dashboard/invites/[token].delete.ts
autonomous: true
requirements:
  - DASH-19
must_haves:
  truths:
    - "Admin pode convidar via email + role; endpoint chama `supabase.auth.admin.inviteUserByEmail` e grava linha em `invites`"
    - "Lista de convites pendentes com opção de revogar (delete)"
    - "Template de email usa redirect para `/confirm` da aplicação"
---

<objective>
Convite por email com role pré-atribuído; usa `auth.admin.inviteUserByEmail` + tabela `invites` existente (fase 04).
</objective>

<tasks>
- `InviteUserForm.vue`: input email + select role. Validação email.
- `POST /api/dashboard/invites`: admin-only; cria linha em `invites` e chama `auth.admin.inviteUserByEmail` com `options.redirectTo=<origin>/confirm`.
- `DELETE /api/dashboard/invites/[token]`: marca como revogado (`expires_at = now()`).
- UI lista invites pendentes (`used_at is null and expires_at > now()`).
</tasks>

<threat_model>
- Service role necessário; roteia via Nitro. Nunca expor no cliente.
- Rate-limit: max 20 convites por hora por admin (simple memory bucket).
</threat_model>
