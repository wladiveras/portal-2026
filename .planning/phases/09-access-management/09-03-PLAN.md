---
phase: 09-access-management
plan: 3
type: execute
wave: 3
depends_on:
  - 1
  - 2
files_modified:
  - supabase/migrations/0007_audit_log.sql
  - app/components/dashboard/access/AuditLogTable.vue
  - server/api/dashboard/audit.get.ts
autonomous: true
requirements:
  - DASH-20
must_haves:
  truths:
    - "Tabela `audit_log (id, actor_id, action, target_type, target_id, meta jsonb, created_at)`"
    - "Triggers em `profiles` e `invites` populam `audit_log` em INSERT/UPDATE/DELETE relevantes"
    - "UI `/dashboard/access` exibe os últimos 50 eventos com filtros por action e actor"
---

<objective>
Dar rasto auditável às operações de acesso. Útil para compliance e debugging.
</objective>

<tasks>
- Migration `0007_audit_log.sql`: cria tabela + triggers.
- Função `public.log_audit(actor uuid, action text, target_type text, target_id uuid, meta jsonb)` para facilitar chamadas imperativas do server.
- `AuditLogTable.vue`: linha por evento, icone por action, relative time.
- `GET /api/dashboard/audit`: admin only; paginação cursor.
</tasks>

<verification>
Manual: mudar role → aparece evento `role_changed` com `actor_id` e `meta { before, after }`.
</verification>
