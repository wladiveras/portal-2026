---

phase: 16-dashboard-layer-split
plan: 3
type: execute
wave: 1
depends_on:

- 16-01-PLAN.md
- 16-02-PLAN.md
files_modified:
- .planning/ROADMAP.md
- .planning/STATE.md
- .planning/codebase/CONVENTIONS.md
- .planning/codebase/STRUCTURE.md
- layers/2-dashboard/README.md
autonomous: true
requirements:
- DDD-07
must_haves:
  truths:
  - "Precedência de merge Nuxt Layers documentada (app > extends > base project)."
  - "Estratégia de override em `app/` (wrappers) vs canônico no layer exemplificada."
  - "Nenhum componente de apresentação contém regra de negócio além de orquestração de UI + $fetch."

---

Validar e documentar a estratégia de override entre `app/` (raiz) e `extends` (`layers/1-base`, `layers/2-dashboard`) para evoluição segura sem surpresas de merge.

- `nuxt.config.ts` define `extends: ['./layers/1-base', './layers/2-dashboard']` — entradas **posterior** no array têm precedência sobre anteriores; `2-dashboard` prevalece sobre `1-base` quando o mesmo path existe em ambos. - Qualquer path presente em **`app/`** prevalece sobre **todos** os layers (fonte canónica de override final do Nuxt 4). - Padrão híbrido aprovado: peças canónicas em `layers/2-dashboard/app/...`; `app/...` com ficheiro mínimo (wrapper) quando se queira ponte explícita (ex.: `TopBar`, `SidebarNav`, `dashboard` layout) **ou** remoção de `app/` para o path ser resolvido só pelo layer (ex.: `leads/`*, `access/*`, `projects/*` copiados para o layer e apagados no `app/`). - `typecheck` e `npm run test` verdes após documentação.

- [x] Documentar precedência em `CONVENTIONS.md` e `STRUCTURE.md`. - [x] Resumir regras e exemplos em `layers/2-dashboard/README.md`. - [x] Sincronizar `ROADMAP.md` / `STATE.md` (fase 16-03 concluída).