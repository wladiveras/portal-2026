---
phase: 15-layers-foundation
plan: 1
type: execute
wave: 1
depends_on:
  - ../14-domain-inventory/14-03-PLAN.md
files_modified:
  - nuxt.config.ts
  - layers/1-base/app/composables/useTheme.ts
  - app/composables/useTheme.ts
  - .planning/STATE.md
autonomous: true
requirements:
  - DDD-04
must_haves:
  truths:
    - "Nuxt usa `extends` com `layers/1-base` e `layers/2-dashboard`."
    - "Composable compartilhado de tema vive no layer base."
    - "Compatibilidade preservada para imports antigos via bridge em `app/`."
---

<objective>
Ativar layers em runtime e iniciar migração de fundações com baixo risco.
</objective>

<tasks>
- Adicionar `extends` no `nuxt.config.ts`.
- Mover `useTheme` para `layers/1-base/app/composables`.
- Manter `app/composables/useTheme.ts` como bridge de re-export.
- Validar com `npm run typecheck` e `npm run test`.
</tasks>
