# 24-02 — Leads filtros por projeto

## Goal

PC-10: leads pipeline pode filtrar por projeto quando coluna existir.

## Tasks

1. Migração `leads.project_id` nullable se ainda não existir + índice.
2. UI filtros em `/dashboard/leads`.

## Verification

- Supabase RLS cobre novo campo (policy review).
