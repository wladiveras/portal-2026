---
phase: 14-domain-inventory
plan: 2
type: execute
wave: 1
depends_on:
  - 14-01-PLAN.md
files_modified:
  - .planning/codebase/APPLICATION-CONTRACTS.md
  - .planning/codebase/DDD-LAYERS-BLUEPRINT.md
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
requirements:
  - DDD-02
must_haves:
  truths:
    - "Cada contexto tem contratos de app-layer (commands/queries) documentados."
    - "Handlers não chamam domínio diretamente no target model."
    - "Audit e authz por use case estão padronizados."
---

<objective>
Definir contratos de application layer que funcionem como API interna estável entre transport e domínio.
</objective>

<tasks>
- Criar `APPLICATION-CONTRACTS.md` com padrão:
  - `Command/Query name`
  - `Input DTO`
  - `Output DTO`
  - `Guards`
  - `Audit hooks`
  - `Repository ports`.
- Cobrir pelo menos:
  - `projects` (create/update/archive, task move, sprint close),
  - `leads` (status update, add note, delete),
  - `access` (invite, role change, disable).
- Incluir convenção de naming:
  - `server/application/<context>/commands/*.ts`
  - `server/application/<context>/queries/*.ts`.
</tasks>

<verification>
- Qualquer handler de `server/api/dashboard/**` pode ser mapeado para 1 command/query sem ambiguidade.
- Contratos incluem estratégia de erro consistente (400/401/403/404/409/500).
</verification>
