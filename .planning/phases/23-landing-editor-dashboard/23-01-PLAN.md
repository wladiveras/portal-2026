# 23-01 — Editor landing no dashboard

## Goal

PC-07: UI em `layers/2-dashboard` para editar blocos (stack editor decidida na implementação).

## Tasks

1. Rota `/dashboard/projects/[id]/landing` + RBAC.
2. Store/composable + chamadas API draft.

## Verification

- Viewer não edita; editor/admin conforme policy.
