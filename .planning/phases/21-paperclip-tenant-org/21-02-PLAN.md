# 21-02 — RLS + repositories + testes contrato tenancy

## Goal

Completar PC-02 + PC-03: políticas RLS coerentes com helpers existentes; sem recursão em `profiles`.

## Tasks

1. Policies SELECT/INSERT/UPDATE por `organization_id` / membership (detalhar com DB owner).
2. Atualizar `server/infrastructure/dashboard/factory.ts` se novos ports.
3. Unit/integration mínimos para gates críticos.

## Verification

- Advisor Supabase sem novos erros críticos.
- Testes novos passam.
