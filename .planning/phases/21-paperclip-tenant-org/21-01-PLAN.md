# 21-01 — Schema organizations + migração projects.organization_id

## Goal

Entregar PC-01 + parte de PC-02: criar `organizations`, ligar `projects` à organização com estratégia de migração segura.

## Tasks

1. Definir DDL em `supabase/migrations/` (numerar próximo após último ficheiro).
2. Atualizar `app/types/database.types.ts` (regen Supabase quando aplicável).
3. Espelhar colunas em `server/db/schema.ts` se contexto agile/ projeto usar Drizzle.
4. Documentar semântica em REQ §6.

## Verification

- Migração aplica sem erro em projeto dev.
- `npm run typecheck` verde.

## Depends on

Nada (além de Phase 20 concluída).
