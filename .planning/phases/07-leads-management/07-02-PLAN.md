---
phase: 07-leads-management
plan: 2
type: execute
wave: 2
depends_on:
  - 1
files_modified:
  - app/components/dashboard/leads/LeadDrawer.vue
  - app/components/dashboard/leads/LeadTimeline.vue
  - app/components/dashboard/leads/LeadStatusPicker.vue
  - server/api/dashboard/leads/[id].get.ts
  - server/api/dashboard/leads/[id]/status.patch.ts
  - server/api/dashboard/leads/[id]/notes.post.ts
autonomous: true
requirements:
  - DASH-13
must_haves:
  truths:
    - "Drawer abre por `?id=` e exibe timeline completa de `lead_events` com agrupamento por dia"
    - "`LeadStatusPicker` emite update otimista com rollback em erro; endpoint valida transições válidas"
    - "Notas são imutáveis (append-only) e incluem autor + timestamp"
---

<objective>
Ligar a listagem ao detalhe operacional: timeline, mudança de status e notas colaborativas.
</objective>

<tasks>
- `LeadDrawer.vue`: `<Transition>` right-side; header com nome/source/status picker; tabs: **Timeline**, **Notas**, **Meta**.
- `LeadTimeline.vue`: agrupa eventos por dia, ícones por tipo, indentação visual para contexto.
- `LeadStatusPicker.vue`: dropdown controlado; envia PATCH e espera 200 antes de confirmar.
- `/api/dashboard/leads/[id].get.ts`: retorna lead + events (limit 200) + notes.
- `/api/dashboard/leads/[id]/status.patch.ts`: valida `new→contacted→qualified→proposal→won|lost` (permite pular para `lost` a qualquer momento); bloqueia retorno inválido.
- `/api/dashboard/leads/[id]/notes.post.ts`: insere nota (tabela `lead_notes` a criar em migration complementar `0004_lead_notes.sql`).
- Migration extra: `lead_notes (id, lead_id, author_id, body, created_at)` + RLS: editor/admin insert e select; viewer só select.
</tasks>
