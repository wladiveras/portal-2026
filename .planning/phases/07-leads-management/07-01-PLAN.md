---
phase: 07-leads-management
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pages/dashboard/leads/index.vue
  - app/components/dashboard/leads/LeadsTable.vue
  - app/components/dashboard/leads/LeadsFilters.vue
  - server/api/dashboard/leads.get.ts
  - app/composables/useLeadsQuery.ts
autonomous: true
requirements:
  - DASH-12
must_haves:
  truths:
    - "Página `/dashboard/leads` lista leads com filtros (periodo, source, utm_*, status, search) e paginação server-side (page + pageSize)"
    - "Filtros preservados em query params para deep-link e refresh"
    - "`LeadsTable` exibe: status chip, display_name, source, utm_campaign, last_seen (relative), ações contextuais"
---

<objective>
Construir a listagem master do pipeline de leads com filtros profissionais e paginação server-side.
</objective>

<tasks>
- `LeadsFilters.vue`: date-range, multi-select de source (via `lead_events.type` distintos), utm fields como combobox populado por `distinct` do backend, status multi-select, search-as-you-type (debounced 350ms).
- `LeadsTable.vue`: colunas sortable (last_seen default desc). Status chip usa cores semânticas: new (muted), contacted (accent-soft), qualified (accent), proposal (amber), won (emerald), lost (rose).
- `useLeadsQuery`: converte filtros → querystring; consume `useFetch('/api/dashboard/leads', { query })`.
- `/api/dashboard/leads.get.ts`: consulta `leads` com joins, aplica RLS (usar cliente autenticado, não service role).
- Linha inteira clicável → abre LeadDrawer (plano 07-02).
</tasks>

<verification>
Unit: `useLeadsQuery` converte filtros em querystring correto. E2E: com fixture de 3 leads, filtra por `status=won` e assert 1 linha.
</verification>
