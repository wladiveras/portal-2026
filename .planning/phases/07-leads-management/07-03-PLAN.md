---
phase: 07-leads-management
plan: 3
type: execute
wave: 3
depends_on:
  - 1
  - 2
files_modified:
  - app/components/dashboard/leads/ExportCsvButton.vue
  - app/utils/csv.ts
  - server/api/dashboard/leads/export.get.ts
  - tests/unit/utils/csv.spec.ts
autonomous: true
requirements:
  - DASH-14
must_haves:
  truths:
    - "`ExportCsvButton` gera CSV com os mesmos filtros aplicados (respeitando limite de 5k linhas); download client-side"
    - "`viewer` vê leads mas não pode exportar (botão desabilitado); `admin` pode apagar leads"
    - "CSV UTF-8 BOM para abrir no Excel; separador `,`; escape RFC 4180"
---

<objective>
Fechar o ciclo operacional: export CSV filtrado e RBAC aplicado em operações sensíveis.
</objective>

<tasks>
- `app/utils/csv.ts`: pure `toCsv(rows, columns)` com escape de aspas e quebras de linha; unit test cobre casos extremos.
- `server/api/dashboard/leads/export.get.ts`: opcional — se dataset pequeno, gerar no cliente; se grande, streaming via `event.node.res`.
- `ExportCsvButton.vue`: usa `useRole().can('edit_leads')` para habilitar; feedback de loading e toast de sucesso/erro.
- Endpoint `DELETE /api/dashboard/leads/[id]` restrito a `admin`.
- Instrumentação: log em `audit_log` quando admin apaga (fase 09 cria a tabela; aqui apenas chamar stub `logAudit`).
</tasks>

<verification>
Unit: `toCsv` escape correto. Integration: login como viewer assert botão desabilitado; login como admin faz download.
</verification>
