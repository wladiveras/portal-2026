# Layer 1 Base

Foundation layer for shared UI and app conventions.

## Intended contents
- design tokens and base theme contracts
- reusable UI primitives
- shared composables/utilities with no dashboard-specific business rules

## Priority
This layer has lower priority than `layers/2-dashboard` and `app/**`.
